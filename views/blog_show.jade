extends layout

block content
  h1= title
  div.article
    div.created_at= article.created_at
    div.title= article.title
    div.body!= article.body
    //div.comment
    //  div.person= article.comment.person
    //  div.comment= article.comment.comment
    div
      form( method="post", action="/blog/addComment/")
        input( type="hidden")
        div
          span Author :
          input( type="text", name="person", id="addCommentPerson")
        div
          span Comment :
          textarea( name="comment", rows=5, id="addCommentComment")
        div#editArticleSubmit
          input(type="submit", value="Send")
